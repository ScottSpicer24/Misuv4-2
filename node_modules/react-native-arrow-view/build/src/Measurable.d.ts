import { PureComponent } from 'react';
import { View } from 'react-native';
export declare type MeasurableProps = {
    onMeasure: (x: number, y: number, width: number, height: number) => void;
};
export declare class Measurable extends PureComponent<MeasurableProps> {
    marker: View | null;
    render(): JSX.Element;
}
